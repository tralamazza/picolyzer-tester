[package]
name = "tester-core"
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"
description = "Pure timing, parsing and waveform-synthesis logic for picolyzer-tester"

[dependencies]

# An internal path dependency, never published and never versioned separately:
# `just release` bumps the firmware only.
[package.metadata.release]
release = false

[lints.rust]
warnings = "deny"
